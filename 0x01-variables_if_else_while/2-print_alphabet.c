#include <stdio.h>

int main() {
    char ch;

    // Print lowercase alphabet using a loop
    for (ch = 'a'; ch <= 'z'; ch++) {
        putchar(ch);
    }

    // Print a new line
    putchar('\n');

    return 0;
}

